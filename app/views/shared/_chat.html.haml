.chat-wrapper.well.well-small
  .chat-messages
  .chat-form
    - if user_signed_in?
      = text_field_tag :chat_message, "", class: "chat-form-input"
    - else
      .chat-not-signed-in
        = t("users.must_sign_in")
.spacer
